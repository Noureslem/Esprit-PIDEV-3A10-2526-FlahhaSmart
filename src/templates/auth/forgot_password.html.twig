{% extends 'base.html.twig' %}

{% block title %}Mot de passe oublie - FlahaSmart{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link href="{{ asset('css/auth.css') }}" rel="stylesheet">
{% endblock %}

{% block body %}
<div class="auth-page">
    <div class="container auth-shell auth-shell-medium">
        <div class="row justify-content-center">
            <div class="col-md-7 col-lg-6">
                <div class="card auth-card">
                    <div class="card-header auth-card-header text-center">
                        <div class="auth-back-wrap">
                            <a class="auth-back" href="{{ path('app_login') }}">
                                <i class="bi bi-arrow-left-circle"></i>Connexion
                            </a>
                        </div>
                        <img src="{{ asset('logo.png') }}" alt="Logo" class="auth-logo">
                        <div class="auth-badge" aria-hidden="true">
                            <i class="bi bi-envelope-paper"></i>
                        </div>
                        <h3 class="auth-title mb-0">Mot de passe oublie</h3>
                    </div>
                    <div class="card-body auth-card-body">
                        {% for message in app.flashes('info') %}
                            <div class="alert alert-info auth-alert">{{ message }}</div>
                        {% endfor %}

                        {% for message in app.flashes('error') %}
                            <div class="alert alert-danger auth-alert">{{ message }}</div>
                        {% endfor %}

                        <p class="text-muted text-center mb-4">
                            Saisissez votre adresse email pour recevoir un code de reinitialisation.
                        </p>

                        <form method="post" action="{{ path('app_forgot_password') }}" class="auth-form">
                            <div class="mb-4 auth-field">
                                <label for="email" class="form-label">Adresse email</label>
                                <div class="input-icon">
                                    <i class="bi bi-envelope" aria-hidden="true"></i>
                                    <input type="email" class="form-control auth-input" id="email" name="email" required autofocus>
                                </div>
                            </div>

                            <button type="submit" class="btn auth-btn w-100">Envoyer le code</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
